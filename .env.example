# ─── Secrets (this file is gitignored) ────────────────────────────────────────
# Copy this to .env and fill in your actual keys/tokens.
# Non-secret config lives in .env.config (committed to the repo).

ANTHROPIC_API_KEY=
TELEGRAM_BOT_TOKEN=
HA_TOKEN=

# Optional: Gemini API key for voice message transcription
GEMINI_API_KEY=
